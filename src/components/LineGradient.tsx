const LineGradient = ({ width = "w-full" }) =>
    // return <div className={`h-0.5 ${width} bg-gradient-rainblue`} /> 
    <div className={`h-0.5 ${width} bg-gradient-green-pink`} />

export default LineGradient;
